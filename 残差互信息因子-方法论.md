# 残差互信息因子（Residual Mutual Information Factor）完整方法论

来源视频：做量化的西蒙《相关系数看不见的股票关系，我用信息论挖出来了》（抖音，2026-06-15，约 13 分钟，10 章）。
本文档在视频公式的基础上，补全了少数视频只讲概念未给闭式的标准公式，并标注来源。

> 标注约定：
> 【视频】= 视频画面中明确给出的公式 / 步骤；
> 【补充】= 该领域标准公式，视频只讲了概念，本文补全闭式以便落地实现。
> 本方法为量化研究流程，不构成任何投资建议。

---

## 0. 核心思想（为什么要做这件事）

衡量两只股票的关系，最常用的是**皮尔逊相关系数 ρ**。但 ρ 只能刻画**线性**关系：

- 如果两只股票存在非线性的共动（涨跌节奏的复杂联动、尾部一起暴动等），ρ 会**低估甚至完全看不到**这种关系。
- **互信息（Mutual Information, MI）** 能捕捉**任意形式**的统计依赖（线性 + 非线性）。

但直接用互信息有个问题：互信息里**本身就包含了线性相关贡献的那一部分**。两只股票只要线性相关强，互信息自然就高，这并不能说明它们有"额外的"非线性关系。

**核心创新——残差互信息 ΔI：**

> ΔI = 实际互信息 − （在相同相关系数下，纯线性关系“应该”有的互信息）

剥掉线性能解释的部分，**剩下的残差就是"相关系数看不见"的纯非线性信息**。再把它聚合成单只股票的因子，就得到一个全新的、与传统价量/相关性因子低相关的 **"非线性共动因子"**。

---

## 完整流程：九步实现

| 步骤 | 名称 | 关键产物 |
|---|---|---|
| 1 | 数据 | 日频收盘价 |
| 2 | 对数收益率 | r_t |
| 3 | 滚动窗口 | 300 天 |
| 4 | 两两相关 | ρ（Pearson）|
| 5 | 经验互信息 | I_emp |
| 6 | 高斯基准 | I_G |
| 7 | 残差互信息 | ΔI |
| 8 | 构网 + 求平均 | mean_ΔI（个股因子）|
| 9 | 单因子测试 | IC / 回测 |

---

## 1. 数据准备

- 标的池：例如某指数成分股（视频用 100 只股票举例）。
- 字段：**日频收盘价** P_t。
- 对齐与清洗：统一交易日历，剔除停牌/新股不足窗口的样本，做前复权。

---

## 2. 价格 → 对数收益率【视频】

把价格序列转成对数收益率：

```
r_t = ln( P_t / P_{t-1} )
```

为什么用对数收益率而不是普通涨跌幅：
- **可加性**：多个时间段的对数收益率可以直接相加（r_1 + r_2 + r_3 = R），便于跨期聚合。
- **统计性质更好**：更接近正态、方差更稳定，更适合做统计建模。

---

## 3. 滚动窗口【视频】

- 用长度 **300 个交易日** 的滚动窗口截取收益率序列。
- 每个窗口内独立计算后续所有指标，窗口向前滚动，得到时间序列上的因子值。
- 窗口长度是平滑度与时效性的权衡：越长越稳但越滞后；300 天约对应 1.2 个交易年。

---

## 4. 两两皮尔逊相关 ρ【视频】

对窗口内任意两只股票 i、j 的收益率序列，计算皮尔逊相关系数：

```
ρ_ij = cov(r_i, r_j) / ( σ_i · σ_j )
```

- 这是**线性关系**的基准度量。
- N 只股票共有 `N×(N−1)/2` 对关系。视频例子：`100 × 99 / 2 = 4950` 对。

---

## 5. 经验互信息 I_emp【视频】

对每一对股票，计算其收益率的**经验互信息**（基于联合分布的离散估计）：

```
I(X;Y) = Σ_{u,v}  p_uv · log( p_uv / ( p_u · p_v ) )
```

- p_uv：X 落在第 u 格、Y 落在第 v 格的联合概率（对收益率做分箱 binning 统计频率得到）。
- p_u、p_v：各自的边缘概率。
- 含义：X 和 Y **不独立的程度**。若完全独立，p_uv = p_u·p_v，则 I = 0；依赖越强 I 越大。**它能捕捉线性 + 非线性的全部依赖。**

实现要点（【补充】）：
- **分箱方式**：等频分箱（quantile binning）通常比等宽更稳健；箱数常取 √n 量级或固定 5–10 箱。
- **估计偏差**：直方图法的 MI 在小样本下有正偏，可用 Miller–Madow 校正或 KSG（k-近邻）估计器降低偏差。
- log 底数取 e（nats）或 2（bits）皆可，全流程保持一致即可。

---

## 6. 高斯基准 I_G【视频概念 + 补充闭式】

**问题**：如果两只股票**只是普通线性相关**（相关系数为 ρ），它们理论上"应该"有多少互信息？这就是高斯基准。

【补充】对一个二元正态分布，互信息有解析闭式（这是信息论里的标准结论，正是视频"高斯基准"的数学形式）：

```
I_G(ρ) = − (1/2) · ln( 1 − ρ² )
```

- 它只依赖相关系数 ρ：ρ 越大，纯线性关系贡献的互信息越大。
- 它代表"在该 ρ 下，**线性能解释的全部互信息上限**"。

---

## 7. 残差互信息 ΔI —— 核心【视频】

把实测互信息减去高斯基准：

```
ΔI_ij = I_emp(i, j) − I_G(ρ_ij)
```

- ΔI > 0：除了线性相关，**还存在相关系数看不见的非线性依赖**。
- ΔI ≈ 0：两者关系基本被线性解释完，没有额外非线性结构。
- 这一步是整套方法的灵魂：**用线性基准做"对照组"，把非线性信号单独剥离出来。**

实现注意（【补充】）：
- I_emp 与 I_G 必须用**同一 log 底数、同一窗口**，否则相减无意义。
- 因估计偏差，ΔI 可能出现小幅负值，可在聚合前做去偏或截断（max(ΔI, 0)）视研究目的而定。

---

## 8. 构建股票信息网络 + 聚合成因子【视频】

### 8.1 关系强弱 → 网络距离

把相关系数转成"距离"，关系越强、距离越近：

```
d_ij = √( 2 × ( 1 − ρ_ij ) )
```

- ρ = 1 → d = 0（最近）；ρ = 0 → d = √2；ρ = −1 → d = 2（最远）。
- 这是把"相关矩阵"变成"距离矩阵"的标准做法，便于做图/聚类。

### 8.2 图过滤：MST 与 PMFG

直接用全连接图（4950 条边）太稠密，需要**图过滤**只留核心结构：

- **MST（最小生成树）**：在保证所有股票都连通的前提下，**只保留最核心的 N−1 条边**。
- **PMFG（平面最大过滤图）**：比 MST 保留更多边、但仍是稀疏的平面图，能保留更丰富的局部结构。
- 过滤后，**同板块的股票会自然成簇**（视频可视化中出现了煤炭、消费、算力、银行、机器人等聚类）。

> 注：网络可视化既可用 ρ 距离建骨架，也可把 ΔI 作为边权来突出"非线性联动"最强的连接。

### 8.3 聚合为单只股票的因子 mean_ΔI【视频】

对每只股票 i，取它与所有其他股票残差互信息的平均，作为该股的**非线性共动因子**：

```
mean_ΔI_i = ( 1 / (N − 1) ) · Σ_{j ≠ i}  ΔI_ij
```

- 直观含义：这只股票在多大程度上**与全市场存在"相关系数看不到的"非线性联动**。
- 视频示例数值：某只股票 mean_ΔI ≈ 0.059（已接通隐藏边 13/29）。
- 因子方向的经济含义需由回测确定（高 mean_ΔI = 更易被市场非线性带动 / 系统性更强，可能是风险暴露，也可能是 alpha 来源）。

---

## 9. 单因子测试（IC / 回测）【视频概念 + 补充定义】

把 mean_ΔI 作为横截面因子，做标准的单因子有效性检验：

【补充】**IC（信息系数）**——因子值与下期收益的横截面相关：

```
IC_t = corr(  factor_t ,  return_{t+1}  )
```

- 用 Pearson 得普通 IC，用 Spearman 秩相关得 **Rank IC**（更稳健，常用）。
- 评估指标：IC 均值、IC 标准差、**ICIR = mean(IC) / std(IC)**、IC 胜率（>0 的比例）。

【补充】**分层回测**：
- 按因子值把股票分成 N 组（如 5 或 10 组），看各组下期收益是否**单调**。
- 看多空组合（Top 组 − Bottom 组）的累计净值、年化收益、夏普、最大回撤。

通过这些，验证"非线性共动因子"是否真的有选股 / 风险刻画能力。

---

## 附：公式速查表

| 步骤 | 公式 |
|---|---|
| 对数收益率 | `r_t = ln(P_t / P_{t-1})` |
| 皮尔逊相关 | `ρ_ij = cov(r_i,r_j)/(σ_i·σ_j)` |
| 经验互信息 | `I(X;Y) = Σ p_uv·log(p_uv/(p_u·p_v))` |
| 高斯基准（补充闭式） | `I_G(ρ) = −½·ln(1−ρ²)` |
| 残差互信息（核心） | `ΔI_ij = I_emp(i,j) − I_G(ρ_ij)` |
| 网络距离 | `d_ij = √(2·(1−ρ_ij))` |
| 个股因子 | `mean_ΔI_i = 1/(N−1)·Σ_{j≠i} ΔI_ij` |
| 信息系数 | `IC_t = corr(factor_t, return_{t+1})` |

---

## 一句话复述整条链路

日频收盘价 → 对数收益率 →（300 天滚动窗口）→ 两两算 ρ 和经验互信息 → 用高斯基准剥掉线性部分得到残差互信息 ΔI → 转成距离建网络（MST/PMFG）并对每只股票求平均 ΔI 得到"非线性共动因子" → IC / 分层回测验证。
