# 标准模型训练 / 评估流程(金融预测信号模型)

> 本仓库(quart)**所有策略模型**的通用准绳:swing 主升浪(run_ml_signals_2026)、first10 连板晋级(2lb)、1to2 首板→2板、regress 4天超额收益、quant_select 多因子 等。
> 目标:**正确性(无泄露)、稳健性(跨时间/环境不崩)、鲁棒性(对扰动不敏感)**。每次改任一模型的 特征/标签/切分/超参,按本流程逐相走;❌ 项未过不得上线。
> 下文以 swing 主升浪模型为体检样例(工具 `swing/run_ml_signals_2026.py --eval`、`swing/model_robustness.py`),其它模型同法套用。

---

## Phase 0 — 数据与标签正确性(Correctness)【一票否决】
- [x] **无未来函数**:特征只用 t 及之前;label 只用 t 之后。基本面 merge_asof backward;label = 前向 60 日 kstar。
- [x] **PIT 对齐**:财务用 ann_date、新闻/研报按决策日截断。
- [⚠] **横截面参照系**:rs20/rsturn/sector_rs 等按池内当日 rank → **池子(--n / 热榜)变化会改变同股特征值**,换池必重训,缓存 key 须含池标识。
- [x] **标签可复现**:主升浪 = 60日内 cumret ≥ k√t(quick0.06/long0.09),kstar 口径固定。
- [x] **源表一键多值去重**:同一 `ts_code+ann_date` 可有多份报告(年报+一季报同日公告),必须显式
  `QUALIFY row_number() OVER (PARTITION BY ts_code, ann_date ORDER BY end_date DESC)=1` 取最新期,
  并用 `kind="mergesort"` 稳定排序。否则取到哪份由 DuckDB 物理行序决定 → 一次增量写库就静默改历史特征值。
  (2026-07 在 swing 实测踩中:34.2% 的行落在重复组、53.0% 取到的不是最新报告、npyoy 中位差 66pct。)
- [ ] 样本去重 / 同股同日不重复;缺失值处理一致(训练=推理)。

## Phase 1 — 数据切分(防泄露)【一票否决】
- [x] **时序 walk-forward**:train < val < test 逐年前推(`_evaluate_wf`),禁用随机 KFold。
- [⚠] **Embargo(关键易漏)**:label 有 60 交易日前瞻窗 → train 末尾与 test 之间必须留 **≥60 交易日 embargo**,否则训练样本的 label 窗口探入测试期 = 泄露。**需核查/补上**。
- [x] **val 早停**:val 独立于 test,early stopping 只看 val。

## Phase 2 — 区分力(Discrimination)
- [x] 逐折 AUC(主看)。**pooled AUC / pooled IC 仅供参考**:不同折是不同模型,分数刻度不可比,
  pooled 值会随 seed 剧烈摆动(2026-07 实测 pooled IC 跨 seed +0.113/+0.101/**-0.077**,而逐折 IC 稳定同号)。
- [x] **top档 lift / 命中率**(实战只买尖端,必看)。
- [x] **rank-IC**(已内置 `--eval`);[ ] IC 随持有期衰减(多 horizon)未做。
- [x] **分位单调性**:按 score 分 5 档,收益应单调递增(已内置 `--eval`)。

## Phase 3 — 概率质量(Calibration)
- [x] **校准曲线 + Brier 分数**(已内置 `--eval`)。跨模型版本 score 绝对值仍不可比(见 Phase 2 pooled 说明),
  故档位门槛必须随模型一起存盘。
- **经验:校准差未必是模型的错,可能是某个特征的测量伪影。** swing 的十分位最大偏离长期 ~0.12(判"偏离偏大"),
  一直被当成"概率不可用、只能排序"的固有限制;2026-07 把 regime 特征从 ΔI 换成平均|rho|(去掉跨年符号翻转的
  伪影)后,偏离直接降到 0.060~0.076、4 个 seed 全判"校准好"。**换特征前先别急着上 isotonic/Platt 后处理**——
  先查有没有特征在不同时期把同一状态映射到不同取值。

## Phase 4 — 过拟合检验(Overfitting)【gap>0.15 一票否决】
- [x] **train vs test AUC gap**(<0.05健康 / 0.05~0.10可接受 / >0.15警惕)。
- [ ] **PBO / CSCV**(回测过拟合概率):多配置(quick/long×pivot×tier×池子×出场)选优时,PBO>0.5 说明"最优"是过拟合(mlfinlab/portfoliolab)。
- [ ] **Deflated / Probabilistic Sharpe**:扣除多次试验的运气后,收益是否仍显著。

## Phase 5 — 稳健性 / 鲁棒性(Robustness)
- [x] **跨市场环境**:牛/熊/震荡分桶(已跑熊市)→ 各环境都不应崩。
- [△] **跨时间稳定性**:逐折指标方差小(已有逐折,未量化方差)。
- [△] **参数敏感性**:对 k / h / tier / 阈值微扰结果平滑(A/B 过,未系统化)。
- [x] **多 seed 方差**:换随机种子 AUC/lift 波动小(看逐折均值,不看 pooled)。
- [ ] **特征鲁棒**:去掉任一 top 特征,性能不崩(probatus/SHAP 消融)。

## Phase 6 — 可解释性(Attribution)
- [x] **SHAP** 特征影响图(`--train` 出 `_shap.png`,中英双标)。
- [ ] 特征重要性随时间漂移监控(某特征突然主导=数据问题预警)。

## Phase 7 — 上线 sign-off 清单
全部通过才可用于实盘信号:
1. ❌ 无任何泄露(Phase 0/1 全绿,含 embargo)。
2. ❌ 逐折 test_AUC 显著 > 0.5 且 top档 lift > 1.5。
3. ❌ 过拟合 gap ≤ 0.15 且(若做了)PBO ≤ 0.5。
4. ❌ 概率校准无系统性偏离。
5. ❌ 跨环境/多 seed 不崩(无某一年/某 seed 暴雷)。

---

## 全流程体检结果(2026-06,quick / kernel)
按本流程跑了一遍,工具落地:`--eval`(rank-IC/分位/校准已内置)+ `model_robustness.py`(PBO/DSR)。

| Phase | 指标 | 结果 | 判定 |
|---|---|---|---|
| 1 泄露 | 补 train↔val embargo 90天后 test_AUC | 0.6880→0.6885 几乎不变 | ✅ 原指标未泄露 |
| 2 区分力 | 逐折 test_AUC / top档 lift | ~0.69 / 1.5–2.0x | ✅ |
| 2 IC | rank-IC 逐折 | +0.187(每折同号,±0.005跨seed) | ✅ 强且稳 |
| 2 单调 | score五档平均涨幅 | 顶档≫底档但中段塌 | △ 极端有效、非严格单调 |
| 3 校准 | Brier / 十分位最大偏离 | 0.177 / 0.101 | △ 概率未校准,只宜排序选top |
| 4 过拟合 | 训练vs测试 gap | +0.09~0.16(2025折偏高) | △ 可接受偏上 |
| 4 PBO | CSCV(9配置×42月) | 0.13 | ✅ 选优非运气 |
| 4 DSR | 最优配置 Deflated Sharpe | 1.00(N=9) | ✅(N偏小,真实更保守) |
| 5 多seed | test_AUC / lift 跨5种子 | ±0.003 / ±0.11 | ✅ 不挑种子 |
| 5 跨环境 | 逐年(2022熊~2025强) | AUC>0.67、lift>1.5 全绿 | ✅ 无环境暴雷 |
| 6 可解释 | SHAP | crowd/市值/指数距高 主导 | ✅ |

**总评:正确性✅(embargo补齐无泄露)、稳健性✅(跨年/seed不崩)、鲁棒性✅(PBO 0.13)。两个待改善:概率校准(Phase3)、中段单调性,均属"锦上添花"而非否决项。**

待补(优先级低):概率校准(isotonic/Platt)、特征消融鲁棒性、参数敏感性系统化、IC多horizon衰减。

## 全流程体检结果(2026-07-25,long / kernel,--n 800 --tier 20 --start 20260622)

触发:修 `fina_indicator/forecast` 同 ann_date 多报告未去重(见 Phase 0)。npyoy 在 FEATS 内且重要度前三,按本流程重评。

| Phase | 指标 | 改前 | 改后 | 判定 |
|---|---|---|---|---|
| 2 区分力 | 逐折 test_AUC 均值 | 0.7049 | **0.7160** | ✅ |
| 2 lift | top20% lift / 命中(基础率12.8%) | 1.92x / 24.5% | **2.05x / 26.5%** | ✅ |
| 2 IC | rank-IC 逐折均值 | +0.184±0.098 | **+0.190±0.104** | ✅ 每折同号 |
| 2 单调 | score五档平均涨幅 | 17<19<20>16<36 中段塌 | 14<17<21<33>23 前四档单调、顶档回落 | △ 有改善,仍非严格单调 |
| 3 校准 | Brier / 十分位最大偏离 | 0.115 / 0.118 | 0.116 / 0.118 | △ 未变,只宜排序 |
| 4 过拟合 | 逐折 train vs test gap | +0.1170 | **+0.1133** | ✅ ≤0.15 |
| 5 多seed | 逐折 test_AUC(42/7/2026) | — | 0.7160/0.7110/0.7078 | ✅ ±0.004 不挑种子 |
| 5 多seed | lift(42/7/2026) | — | 2.05x/1.94x/2.04x | ✅ |
| 6 可解释 | 特征重要度 | npyoy 第3(54.2) | **npyoy 第1**(37.2) | ✅ 口径修对后模型更用它 |

逐折不是一致改善:折2 `0.6765→0.7174`、折5 `0.7307→0.7527` 升,折3 `0.7056→0.6981`、折4 `0.7069→0.6959` 降。
**2升2降但均值/lift/IC 均改善 → 读作"特征变干净后小幅正向",不宜宣称显著。**

**新发现(已回填 Phase 2/3):pooled 指标不可信。** pooled AUC 跨 seed `0.6653/0.6465/0.5380`、
pooled rank-IC `+0.113/+0.101/-0.077`,而逐折全部稳定同号。成因是各折是各自训的模型、分数刻度不可比,
拼一起排序即失效。推论:**score 绝对值只在单一模型版本内有意义**,故 swing 的档位门槛取训练期分数分布
(`score_ref`)随模型一起存盘、重训才更新——写死常数会在换模型时榜单全乱(本次重训门槛 0.1504→0.1695)。

部署:`--start 20260622 --train`,train_cutoff 与打分起点对齐(此前模型训到 20260721 却打 20260622 起的信号,
中间一个月是样本内)。训练4529/val1206,best_iter=45,训练AUC 0.8324 / valAUC 0.7638,gap +0.0686 ✅。

Phase 7 sign-off(该轮):1无泄露✅ 2 AUC/lift✅ 3 gap 0.113✅ 4校准△(仅排序用) 5多seed✅ → 可上线,概率不可直接解读。
(校准 △ 已在同日的下一轮 regime 特征替换中转 ✅,见下节。)

### 追加体检:regime 特征 ΔI → 平均|rho|(2026-07-25 同日,已上线)

`crowd`(残差互信息 ΔI)查明是平均|rho| 的反向代理(corr −0.60),且皮尔逊 ρ 在肥尾期被极端共动抬高、
与秩基础的经验 MI 口径不一致 → 高|rho| 区过度减除、ΔI 跨年符号翻转(2025 全年为负)。换成 `arho`(平均|rho|)
后 4 seed 配对评估:

| Phase | 指标 | ΔI(旧) | **平均\|rho\|(新)** | 判定 |
|---|---|---|---|---|
| 2 区分力 | 逐折 test_AUC 4seed均值 | 0.7116 | **0.7199** | ✅ 4/4 配对为正,新的最差 seed > 旧的最好 seed |
| 2 lift | top20% 命中 / lift | 25.7% / 2.015x | **26.4% / 2.043x** | ✅ 命中 4/4 正,lift 3升1降 |
| 2 IC | rank-IC 逐折均值 | +0.183 | **+0.199** | ✅ |
| **3 校准** | 十分位最大偏离 | 0.118 / 0.126 | **0.060~0.076(4/4)** | **✅ 由 △ 转 ✅** |
| 4 过拟合 | 逐折 gap 4seed均值 | +0.1167 | **+0.1132** | ✅ 略降 |
| 5 多seed | 逐折 test_AUC 极差 | 0.0082 | 0.0071 | ✅ |

被否的变体:两者并用(22特征)三项全面最差(AUC 0.7132/lift 1.98x/校准偏离 0.166)——ΔI 的伪影是噪声,
给模型两个高相关输入会让它在噪声上分裂。**教训:相关性不高(−0.60)不等于两者都该留。**

**Phase 3 结论更新:概率校准 △ → ✅**(十分位最大偏离 ≤0.076,4 seed 一致)。但 Phase 2 的
"score 绝对值跨模型版本不可比"仍然成立,档位门槛照旧必须随模型存盘。

## 现状小结(2026-06)
- **已具备**:无未来函数、walk-forward、AUC/lift/命中、过拟合 gap、熊市验证、SHAP。基础扎实。
- **最该补(按优先级)**:
  1. **Embargo ≥60 交易日**(Phase 1)—— 正确性,最高优先,可能存在泄露。
  2. **rank-IC + 校准/Brier**(Phase 2/3)—— 零安装,直接复用 OOS 预测。
  3. **PBO + Deflated Sharpe**(Phase 4)—— 你 A/B 极多,这是防"选出来的最优=过拟合"的核心。
- 推荐库:`alphalens-reloaded`(因子IC)、`mlfinlab`/`portfoliolab`(PBO/DSR/purged-CV)、`probatus`(分类器稳健性)、`sklearn`(校准,已装)。
