# 自建「因子研究 UI」设计笔记

目标:把「**输入一个因子 → 一键体检 → 判断真假**」这条链做成前端。基于现有 React + FastAPI + DuckDB。

## 1. 输入层(定义"测什么")
- **因子定义**:表达式或一段计算(如 `alpha144 = sumif(|ret|/amount, ret<0, 20)`)。
  - 先支持**选内置因子**;进阶做**表达式解析**(`rank(ts_sum(...))`,可参考 qlib / WorldQuant 算子集)。
- **股票池**:中证500/1000/2000/全A(已有成分表)。
- **区间 + 调仓频率**(日/周/月)。
- **⭐成分口径开关:PIT(当时成分) vs 当前名单** —— 识破幸存者偏差的关键,必须内置。

## 2. 计算层(后端,喂给前端的数据)
DuckDB 上向量化算:
- **IC 序列**:每期因子值 vs 未来收益的 Rank IC(Spearman)→ IC 均值、ICIR、IC 胜率、t 值。
- **分层收益**:因子分 5/10 组,每组净值 + 年化 → 看**单调性**。
- **多空组合**:Top - Bottom 的净值、年化、夏普、最大回撤。
- **换手率**:每期持仓变动 → 可交易性/成本。
- **IC 衰减**:未来 1/5/10/20/60 日的 IC → 因子有效持续多久。
- **因子暴露/相关**:与市值、行业、动量/反转/波动的相关 → 是不是市值马甲。

## 3. 分析视图(前端,recharts 够用)
标准"因子体检表"(alphalens 那套,中文):
1. **分层净值**(5-10 条线)+ 分层年化柱状(看单调)
2. **多空净值** + 指标卡(年化/夏普/回撤/IC/ICIR)
3. **IC 时序**(柱)+ 累计 IC 曲线
4. **IC 衰减**(不同持有期)
5. **换手率**时序
6. **分年份**分层收益热力图(看是不是每年都稳)

## 4. ⭐稳健性 / 反作弊检验(比视频强的地方)
把踩过的坑做成一键功能:
- **PIT vs 当前名单** 一键对照(演示幸存者偏差,alpha144 上 -2%→+28%)
- **流动性门槛**:剔除低成交额后重算(alpha144 那种由正转负)
- **剔除次新/ST/停牌**、**涨跌停不可成交**约束
- **样本内/外切分**(walk-forward),别整段回测
- **成本敏感性**:双边费 + 冲击成本扫描
- **子区间稳定性**:分年 / 牛熊分段看因子还在不在

## 5. 工程层
- 后端:FastAPI 加 `/api/factor`(入参:因子/池/区间/口径 → 出上面所有 JSON)。因子结果可缓存(按 因子+池+区间 存 DuckDB,像 concept_signals)。
- 前端:一个「因子研究」tab,顶部输入区 + 下方图表网格。
- 因子库:先内置几个(alpha144、动量、反转、换手),留表达式输入框扩展。

## MVP vs 完整版
- **MVP(1-2 天)**:内置因子 + 池 + 区间 → **分层净值 + 多空净值 + IC均值/ICIR + 换手** + **PIT开关**。
- **完整版**:加表达式解析、IC衰减、行业中性化、成本扫描、因子相关矩阵。

## 建议
- 别从零造分层/IC 计算,直接用 **`alphalens-reloaded`** 出这些指标,自己只做
  「DuckDB 取数 → alphalens 算 → JSON → React 画中文图」的壳。
- 稳健性检验(PIT / 流动性 / 成本 / 子区间)是 alphalens 没有的,**自己加——这几个最有价值**。
- 参考中文平台交互:FinHackCN/finhack;因子研究内容:hugo2046/QuantsPlaybook。

## 落地起点(待定)
MVP:在 webapp 加「因子研究」页,拿 `alpha144` 当第一个内置因子,出
**分层净值 + 多空 + IC + 换手 + PIT对照**,跑通后再往里加因子。
